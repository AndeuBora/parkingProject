<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="adminQnaReply">

	<!-- 댓글 조회 -->
	<select id="adminQnaReplyList" parameterType="com.parking.qnaBoard.vo.AdminQnaReplyVO"
		resultType="com.parking.qnaBoard.vo.AdminQnaReplyVO">
		select replynum, qnanum, adminid, replycontent,
		to_char(replywritedate,
		'YYYY-MM-DD') as replywritedate
		from reply
		where
		qnanum = #{qnaNum}
		order by replynum desc
	</select>

	<!-- 댓글 등록 -->
	<insert id="adminQnaReplyInsert" parameterType="com.parking.qnaBoard.vo.AdminQnaReplyVO">
		insert into reply
		(
		replynum, qnanum, adminid, replycontent
		) values (
		replynum_seq.nextval,
		#{qnaNum}, #{adminId}, #{replyContent}
		)
	</insert>

	<!-- 댓글 수정 -->
	<update id="adminQnaReplyUpdate" parameterType="com.parking.qnaBoard.vo.AdminQnaReplyVO">
		update reply
		set replycontent = #{replyContent},
		replywritedate = sysdate
		where replynum = #{replyNum}
	</update>

	<!-- 댓글 삭제 -->
	<delete id="adminQnaReplyDelete" parameterType="com.parking.qnaBoard.vo.AdminQnaReplyVO">
		delete from reply where replynum = #{replyNum}
	</delete>

</mapper>
