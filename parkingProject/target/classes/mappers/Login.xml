<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="login">

	<select id="getPwd" resultType="String" parameterType="com.parking.member.vo.MemberVO">

		select memberPwd
		from member
		where memberId=#{memberId}


	</select>

	<select id="memberData"  parameterType="com.parking.member.vo.MemberVO" resultType="com.parking.member.vo.MemberVO">

	select memberNum as memberNum,
	memberId as memberId,
	memberPhone as memberPhone,
	memberType as memberType
	from member
	where memberId=#{memberId}
	</select>
</mapper>
